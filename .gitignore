# Build output (generado por `npm run build`)
public/

# Dependencias y logs
node_modules/
npm-debug.log*
.vercel

# Fichas fuente del portafolio (contienen datos de contacto y notas internas
# "no publicar"): quedan solo en local, nunca en el repo público.
content/fichas/
